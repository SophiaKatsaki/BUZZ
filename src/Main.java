/**
 *
 * This class just creates an informationGUI object and then
 * the method start() that the object has,is called, in order
 * for the opening frame to be set as visible and practically
 * for the game to start.
 *
 */

public class Main
{
    public static void main(String[] args) {
        InformationGUI BUZZ = new InformationGUI();
        BUZZ.start();
    }
}